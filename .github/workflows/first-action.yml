name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Pring greeting
        run: echo "Hello, world"
      - name: Print goodbye
        run: echo "Goodbye, world"
